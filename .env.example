# Offline development provider. No credentials required.
IRIS_MODEL_PROVIDER=mock

# Used only by the native Rust provider client. Never commit a real value.
IRIS_API_KEY=
IRIS_BASE_URL=https://example.com/v1
IRIS_MODEL=your-model
