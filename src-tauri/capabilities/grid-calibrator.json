{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "grid-calibrator",
  "description": "Grid calibration window access only",
  "windows": ["grid-calibrator"],
  "permissions": [
    "allow-grid-calibrator-commands",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "core:window:allow-hide",
    "core:window:allow-is-visible"
  ]
}
